# Pre-commit fence. Agent hooks block --no-verify; see AGENTS.md.
# Installed by the prepare script on pnpm install.
pre-commit:
  piped: true
  commands:
    check:
      priority: 1
      run: pnpm check
    test:
      priority: 2
      run: pnpm test
